# Данные VPN / панели (ключи, туннели) — только на сервере
awg-data/
awg-data-v2-backup/

# Рабочий файл с секретами (пароль веб-UI, хэш и т.д.).
# В репозитории хранится только шаблон .env.example — скопируйте его в .env локально:
#   cp .env.example .env
# Затем отредактируйте .env под свой сервер. Файл .env в git не коммитится.
.env
